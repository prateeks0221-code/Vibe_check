# torch + torchvision already in base image (pytorch/pytorch:2.1.0-cuda12.1)
# numpy pinned <2 — pytorch 2.1 not compatible with numpy 2.x
numpy>=1.24.0,<2.0.0
ultralytics>=8.2.0
opencv-python-headless>=4.8.0
redis>=5.0.0
requests>=2.31.0
python-dotenv>=1.0.0
paho-mqtt>=1.6.0,<2.0.0
lap>=0.5.12            # ByteTrack dep — baked in here so Ultralytics never auto-downloads at runtime
